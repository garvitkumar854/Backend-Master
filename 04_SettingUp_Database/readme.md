# Initialize blank server app

# created app.js and server.js

# Installed mongoose, and created cluster on mongodb and connected through the mongodb compass

# creating seperate folder `db` to seperate the db related code

# require mongoose and connect to db then exports the connectdb

# import the actual connect db then call the function

# create models folder under src and create a sepearate model.js file for schema

# and creates the schema for data

# Create model from the schema => mongoose.model("note", noteSchema) // particular node of notes is called note (recommended - optional)
